<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <h2><a href="/new">Check new messages!</a></h2>
    <ul>
      <% messages.forEach(msg => { %>
      <li>
        <h3><%= msg.user %></h3>
        <p><%= msg.text %></p>
        <p><%= msg.added %></p>
      </li>
      <% }); %>
    </ul>
  </body>
</html>
